#include <cstdio>
#include <algorithm> // sort() function
#define MAX 100
using namespace std;
int main()
{
int i, data[MAX];
for(i=0; i<10; i++)
scanf("%d", &data[i]);
sort(data, data+10);
for(i=0; i<10; i++)
printf("%d ", data[i]);
return 0;
}
